datasource.driverClassName=${datasource.driverClassName}
datasource.url=${datasource.url}
datasource.username=${datasource.username}
datasource.password=${datasource.password}
datasource.public.key=${datasource.public.key}

datasource.max.active=${datasource.max.active}
datasource.initial=${datasource.initial}
datasource.min.idle=${datasource.min.idle}
datasource.max.wait=${datasource.max.wait}

document.generate.endpoint=${document.generate.endpoint}
document.generate.access.key=${document.generate.access.key}
document.generate.access.secret=${document.generate.access.secret}
document.generate.app.key=${document.generate.app.key}

object.storage.type=${object.storage.type}
aliyun.oss.access.key=${aliyun.oss.access.key}
aliyun.oss.access.secret=${aliyun.oss.access.secret}
aliyun.oss.endpoint=${aliyun.oss.endpoint}
aliyun.oss.bucket.name=${aliyun.oss.bucket.name}
aliyun.oss.directory.name=${aliyun.oss.directory.name}