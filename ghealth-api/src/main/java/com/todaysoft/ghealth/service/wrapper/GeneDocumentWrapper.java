package com.todaysoft.ghealth.service.wrapper;

import org.springframework.stereotype.Component;

@Component
public class GeneDocumentWrapper
{
    /*  public List<GeneDocument> wrap(List<com.todaysoft.ghealth.mybatis.model.GeneDocument> records)
    {
        if (CollectionUtils.isEmpty(records))
        {
            return Collections.emptyList();
        }
    
        GeneDocument geneDocument;
        List<GeneDocument> geneDocuments = new ArrayList<GeneDocument>();
    
        for (com.todaysoft.ghealth.mybatis.model.GeneDocument record : records)
        {
            geneDocument = new GeneDocument();
            wrap(record, geneDocument);
            geneDocuments.add(geneDocument);
        }
    
        return geneDocuments;
    }*/
    
}
